export const initialize = jest.fn().mockResolvedValue(true);
export const requestPermission = jest.fn().mockResolvedValue([]);
export const aggregateRecord = jest.fn().mockResolvedValue({});
export const aggregateGroupByPeriod = jest.fn().mockResolvedValue([]);
export const getSdkStatus = jest.fn().mockResolvedValue(0);
